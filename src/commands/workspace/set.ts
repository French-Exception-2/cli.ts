(async () => {
    // const opModule = require('./../../../operations/workspace/set');
  
    // exports.command = 'workspace:set';
    // exports.desc = 'Workspace set current';
    // exports.builder = opModule.builder;
    // exports.handler = async function (argv: WorkspaceSetArgv ) {
    //   const op = opModule.handle;
    //   await op(argv);
    // };
  })();
  